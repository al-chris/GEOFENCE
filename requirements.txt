filterpy>=1.4.5
shapely>=2.0.0
# Keep NumPy < 2: the venv shadows the ROS 2 system NumPy, and apt-installed
# transforms3d 0.3.1 (used by nmea_navsat_driver) calls np.maximum_sctype,
# which was removed in NumPy 2.0. Ubuntu 24.04 / Jazzy ship NumPy 1.26.4.
numpy>=1.24.0,<2.0
matplotlib>=3.1.0
PyYAML>=6.0
lgpio>=0.2.2.0
